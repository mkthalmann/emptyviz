% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme-bayes-halfeye.R
\name{plot_ridge_hdi}
\alias{plot_ridge_hdi}
\title{Ridgeline plot of posterior marginal means/SDs, one row per category}
\usage{
plot_ridge_hdi(
  data,
  category,
  value = .value,
  facet = NULL,
  facet_nrow = NULL,
  facet_ncol = NULL,
  facet_scales = "fixed",
  hline = NULL,
  hline_color = colors[2],
  value_transform = identity,
  value_limits = NULL,
  value_breaks = waiver(),
  category_reorder = TRUE,
  xlab = "Parameter",
  ylab = "Posterior marginal means ±HDI<sub>95</sub> ±ETI<sub>50;90;95</sub>",
  ...
)
}
\arguments{
\item{data}{A data frame of long-format posterior draws.}

\item{category}{Unquoted column identifying each category (one row per
panel/facet row).}

\item{value}{Unquoted column of posterior draws (default \code{.value},
matching \code{tidybayes::gather_emmeans_draws()}'s own column name).}

\item{facet}{Optional unquoted column to facet by.}

\item{facet_nrow, facet_ncol, facet_scales}{Passed to \code{facet_wrap()} when
\code{facet} is given.}

\item{hline}{Optional y-intercept for a dashed reference line (\code{NULL}
omits it).}

\item{hline_color}{Color of the reference line.}

\item{value_transform}{Applied to \code{value} before plotting (e.g. \code{exp} for
a sigma submodel estimated on the log scale).}

\item{value_limits, value_breaks}{Passed to \code{scale_y_continuous()}.}

\item{category_reorder}{\code{TRUE} (default) sorts categories by their mean
transformed \code{value} (descending) via \code{\link[forcats:fct_reorder]{forcats::fct_reorder()}}; \code{FALSE}
keeps whatever factor-level order \code{category} already has.}

\item{xlab, ylab}{Axis labels.}

\item{...}{Passed through to \code{\link[=layer_halfeye_hdi]{layer_halfeye_hdi()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Every category stacked as a row in one shared-scale panel (optionally
faceted, but with a common axis across facets by default via
\code{facet_scales = "fixed"}) - for when the categories themselves are what's
being compared.
}
\details{
\code{category}/\code{value} are unquoted columns, as in the usual ggplot2 idiom.
\code{value} should be long-format posterior draws (one row per draw per
category), e.g. from \code{tidybayes::gather_emmeans_draws()}.
}
