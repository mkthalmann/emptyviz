% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-believe-projection.R
\docType{data}
\name{believe_projection_draws}
\alias{believe_projection_draws}
\title{Posterior marginal draws from the believe-projection model}
\format{
A tibble with 64,000 rows and 6 variables:
\describe{
\item{scenario}{\code{"true"}, \code{"false"}, \code{"undef"}, or \code{"critical"}.}
\item{negation}{\code{"with"} or \code{"without"}.}
\item{trigger}{\code{"*again*"} or \code{"*stop*"} (markdown-italicized).}
\item{dpar}{\code{"mu"} (the judgment submodel) or \code{"sigma"} (the
distributional scale submodel, log scale).}
\item{.draw}{Posterior draw index.}
\item{.value}{The draw's value.}
}
}
\source{
See \link{believe_projection}. Model fit in
\code{believe-projection/scripts/belproj-paper.R}; draws extracted by
\code{data-raw/believe-projection-posteriors.R}.
}
\usage{
believe_projection_draws
}
\description{
Long-format posterior draws from the distributional \code{brms} model fit to
\link{believe_projection} (\code{judgment ~ negation*scenario*trigger},
\code{sigma ~ negation*scenario + trigger}), one row per posterior draw per
\code{scenario} x \code{negation} x \code{trigger} cell x submodel. Extracted via
\code{emmeans(mod, ~scenario*negation*trigger)} (and again with
\code{dpar = "sigma"}) piped through \code{tidybayes::gather_emmeans_draws()}, then
thinned to 2,000 draws per cell per submodel - see
\code{data-raw/believe-projection-posteriors.R}.
}
\details{
\code{dpar == "sigma"} draws are on the log scale (the model's own link
function for that submodel); back-transform with \code{exp()} before plotting,
e.g. via \code{plot_ridge_hdi()}'s \code{value_transform = exp}.

Pivoting \code{dpar} wide and joining on \code{.draw} (plus the condition columns)
recovers the \emph{paired} location/scale draws \code{\link[=plot_location_scale]{plot_location_scale()}}
requires - the same pairing \code{vignette("bayesian-plots")} builds by hand.
}
\seealso{
\link{believe_projection}, \link{believe_projection_coef_draws},
\link{believe_projection_bf}, \code{vignette("bayesian-plots")}.
}
\keyword{datasets}
