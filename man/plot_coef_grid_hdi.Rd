% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme-bayes-halfeye.R
\name{plot_coef_grid_hdi}
\alias{plot_coef_grid_hdi}
\title{Free-scale grid of posterior coefficients, one panel per category}
\usage{
plot_coef_grid_hdi(
  data,
  category,
  value = .value,
  ncol = 4,
  nrow = NULL,
  hline = 0,
  hline_color = mt_colors[2],
  ylab = "Posterior coefficients ±HDI<sub>95</sub> ±ETI<sub>50;90;95</sub>",
  ...
)
}
\arguments{
\item{data}{A data frame of long-format posterior draws.}

\item{category}{Unquoted column identifying each coefficient (one facet
panel per level).}

\item{value}{Unquoted column of posterior draws (default \code{.value}).}

\item{ncol, nrow}{Passed to \code{facet_wrap()}.}

\item{hline}{Y-intercept for a dashed reference line; defaults to \code{0}
(the null-effect reference line), pass \code{NULL} to omit it.}

\item{hline_color}{Color of the reference line.}

\item{ylab}{Axis label.}

\item{...}{Passed through to \code{\link[=layer_halfeye_hdi]{layer_halfeye_hdi()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Each category gets its own free-scale panel in a grid - for coefficient
posteriors, which differ wildly in natural magnitude, so a shared axis
would flatten the small ones.
}
\details{
Unlike \code{\link[=plot_ridge_hdi]{plot_ridge_hdi()}}, the category label is shown as the facet
strip, not an axis, so there's no \code{fct_reorder()} step - pass \code{category}
already in the order you want the facets to appear (e.g. via
\code{forcats::fct_inorder()} on however you built the long-format coefficient
draws).
}
\examples{
mu_coef_draws <- subset(believe_projection_coef_draws, dpar == "mu")
plot_coef_grid_hdi(mu_coef_draws, category = coef, ncol = 4)
}
