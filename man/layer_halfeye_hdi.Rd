% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme-bayes-halfeye.R
\name{layer_halfeye_hdi}
\alias{layer_halfeye_hdi}
\title{The recurring halfeye (HDI slab + ETI point-interval) layer bundle}
\usage{
layer_halfeye_hdi(
  slab_widths = c(0.95, 0.999),
  interval_widths = c(0.5, 0.9, 0.95),
  slab_limits = NULL,
  pointinterval_limits = NULL,
  scale = 1,
  dodge_width = 0.4,
  gap = 0.02,
  n = NULL,
  point_size = 1.5,
  fill_colors = NULL,
  interval_color = NULL
)
}
\arguments{
\item{slab_widths}{HDI width(s) for the slab shading, passed to
\code{stat_slab()}'s \code{.width}.}

\item{interval_widths}{ETI width(s) for the point-interval, passed to
\code{stat_pointinterval()}'s \code{.width}.}

\item{slab_limits, pointinterval_limits}{Passed through to \code{stat_slab()}'s/
\code{stat_pointinterval()}'s own \code{limits} argument (\code{NULL} by default, i.e.
ggdist's own auto behavior). Not the same as \code{value_limits} in
\code{\link[=plot_ridge_hdi]{plot_ridge_hdi()}}/\code{\link[=plot_coef_grid_hdi]{plot_coef_grid_hdi()}}, which is a real axis-scale
limit.}

\item{scale}{Slab height, passed to \code{stat_slab()}.}

\item{dodge_width}{Dodge width shared by the slab and point-interval.}

\item{gap}{Point-interval offset from the slab baseline, as a fraction of
panel height (see Details).}

\item{n}{Passed to \code{stat_slab()}'s \code{n} (density resolution), if given.}

\item{point_size}{Size of the point-interval's point.}

\item{fill_colors}{Length-2 fill colors for the two innermost HDI widths;
defaults to the package's \link{colors} palette.}

\item{interval_color}{Color of the point-interval; defaults to
\code{colors[1]}.}
}
\value{
A list of \code{ggplot2}/\code{ggdist} layers, scales, and guides.
}
\description{
The \code{stat_slab()}+\code{stat_pointinterval()} block that recurs, nearly
verbatim, across posterior-summary plots: an HDI-shaded density
(\code{\link[ggdist:stat_slab]{ggdist::stat_slab()}}) plus a multi-width ETI point-interval
(\code{\link[ggdist:stat_pointinterval]{ggdist::stat_pointinterval()}}). Returns a list of layers/scales to add
to a \code{ggplot()} that already has a discrete category on one axis and a
continuous draws column on the other - orientation (vertical vs.
flipped) is the caller's job via \code{coord_flip()}, not this function's.
Used internally by \code{\link[=plot_ridge_hdi]{plot_ridge_hdi()}} and \code{\link[=plot_coef_grid_hdi]{plot_coef_grid_hdi()}}; exposed
standalone for building a custom layout around the same visual grammar.
}
\details{
\code{scale} (slab height) and \code{dodge_width} have no single correct default -
pass whatever fits your panel count and coefficient spread.

\code{gap} is how far the point-interval is shifted away from the slab's
baseline, as a target fraction of \emph{panel height} (see
\code{position_dodge_gap()}'s own comment for why this - not a row-unit
fraction - is the right invariant to hold constant across callers with
different category counts per panel). Default \code{.02} (~2\% of panel
height) looks right both for many-categories-sharing-one-panel layouts
and one-category-per-panel grids; \code{0} reproduces a touching layout with
no gap at all.
}
