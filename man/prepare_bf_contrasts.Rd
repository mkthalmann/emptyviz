% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme-bayes-bf-forest.R
\name{prepare_bf_contrasts}
\alias{prepare_bf_contrasts}
\title{Split a bayestestR-style contrast string into left/right columns}
\usage{
prepare_bf_contrasts(
  data,
  contrast = contrast,
  value = NULL,
  strip = " NA$",
  pairs = NULL
)
}
\arguments{
\item{data}{\code{bayesfactor_rope()} output coerced to a data frame (or any
data frame with a \code{contrast} column in that \code{"<left> - <right>"}
format).}

\item{contrast}{Unquoted column holding that raw string (default
\code{contrast}, matching bayestestR's own column name).}

\item{value}{Optional unquoted column - typically the log-BF/BF column
itself - to sign-flip on any row matched via a swapped pair (see
\code{pairs}), so a positive value still means whatever the caller's
first-named condition supports. \code{NULL} (default) leaves every column
as-is; if any pair needed swapping and no \code{value} is given, a warning
names how many rows were affected.}

\item{strip}{A regular expression removed from each side after splitting
(default \code{" NA$"}, since a reference grid that marginalizes over a
grouping variable leaves a literal trailing "NA" token in emmeans'
generated label). \code{NULL} disables this.}

\item{pairs}{Optional list of \code{c(left, right)} character pairs (matched,
after trimming whitespace, against the split \code{.left}/\code{.right}
columns). When given: keeps only matching rows, \emph{in the order \code{pairs}
lists them}, and errors - naming exactly which pair - if any requested
pair isn't found. \code{NULL} (default) keeps every row, unfiltered and in
\code{data}'s own order.

A pair's order does NOT have to match bayestestR's own \code{"<left> - <right>"} order for that contrast - a pair that only matches once
\code{.left}/\code{.right} are swapped is accepted the same as a direct match;
\code{.left}/\code{.right}/\code{.contrast_label} are then set to the \emph{requested}
order regardless of which way \code{data} actually had it.

Two entries can therefore resolve to the \emph{same} row of \code{data} - either
as an outright repeat, or as the two directions of one contrast
(\code{c("a", "b")} and \code{c("b", "a")}, the second relabeled and
sign-flipped). Both are allowed and warn: each becomes its own output
row, so a forest plot built from the result shows one estimate as
several, which reads as several independent ones. Drop the duplicate if
that wasn't the intent.}
}
\value{
\code{data}, with \code{.left}, \code{.right}, and \code{.contrast_label} columns
added (and, if \code{pairs} was given, filtered/reordered/relabeled to
match).
}
\description{
Splits \code{bayestestR::bayesfactor_rope()}'s auto-generated \code{contrast}
column (format: \code{"<left> - <right>"}, reliably delimited by \code{" - "}
regardless of how many crossed factors make up each side) into separate
\code{.left}/\code{.right} columns, and optionally filters + reorders down to just
the pairs the caller actually wants to show.
}
\examples{
bf <- data.frame(
  contrast = c("a - b NA", "c - d NA", "b - a NA"),
  log_BF = c(1.2, -0.3, 0.8)
)
prepare_bf_contrasts(bf)
prepare_bf_contrasts(bf, value = log_BF, pairs = list(c("a", "b")))
}
