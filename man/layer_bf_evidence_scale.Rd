% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme-bayes-bf-forest.R
\name{layer_bf_evidence_scale}
\alias{layer_bf_evidence_scale}
\title{Weak-evidence region + directional arrows for a log-BF axis}
\usage{
layer_bf_evidence_scale(
  orientation = c("x", "y"),
  weak_threshold = log(3),
  weak_label = "Weak evidence region",
  weak_fill = NULL,
  weak_color = NULL,
  direction_labels = NULL,
  direction_colors = NULL,
  range = NULL,
  secondary_axis = FALSE,
  secondary_breaks = c(1, 2, 5, 15, 50, 150),
  secondary_name = "Bayes factor (BF)"
)
}
\arguments{
\item{orientation}{\code{"x"} (log-BF on the x-axis, e.g. a forest plot) or
\code{"y"} (log-BF on the y-axis, e.g. a continuous-x sensitivity sweep).}

\item{weak_threshold}{Half-width of the shaded band, in log-BF units.
Default \code{log(3)}, the Jeffreys/Lee "weak/anecdotal evidence" convention
\itemize{
\item the same convention \code{bayestestR::bayesfactor_rope()}'s BFs are on.
}}

\item{weak_label}{Text for the shaded band (\code{NULL} omits it). Anchored
via \code{Inf} + hjust/vjust to the relevant panel edge rather than a
data-dependent position.}

\item{weak_fill, weak_color}{Fill/text color for the shaded band and its
label; default to \code{colors5[3]}.}

\item{direction_labels}{\code{c(positive, negative)} - text naming what a
positive/negative log-BF supports (e.g. \code{c("supports a real difference", "supports practical equivalence")} for a ROPE comparison).
\code{NULL} (default) omits the arrows and labels entirely.}

\item{direction_colors}{Length-2 colors for the positive/negative
arrows/labels; defaults to \code{colors[1:2]}.}

\item{range}{\code{c(min, max)} the arrows should span along the BF axis.
Only used when \code{direction_labels} is given; \code{NULL} falls back to a
generic \code{c(-1, 1) * weak_threshold * 3}.}

\item{secondary_axis}{\code{FALSE} (default) or \code{TRUE} to add a linear-BF
secondary axis via \code{sec.axis = dup_axis(...)} at \code{secondary_breaks}.}

\item{secondary_breaks}{Breaks for the secondary linear-BF axis.}

\item{secondary_name}{Axis title for the secondary linear-BF axis.}
}
\value{
A list of \code{ggplot2}/\code{ggarrow} layers and annotations.
}
\description{
A shaded "weak evidence" region + optional directional arrows/labels +
optional secondary linear-BF axis, for any plot with a log Bayes Factor
valued axis. Composable (a list of layers/annotations), not a full
plot-builder - add it to a ggplot you build yourself, the way
\code{\link[=layer_halfeye_hdi]{layer_halfeye_hdi()}} works.
}
\details{
Both the arrows and their direction labels are Inf-anchored to the same
panel edge \code{weak_label} uses (top, for \code{orientation = "x"}; right, for
\code{"y"}), with the labels sitting right at each arrow's tip via
vjust/hjust nudges. Inf-anchoring costs no reserved space at all - no
scale expansion or \code{coord_cartesian(clip = "off")} is needed beyond what
\code{weak_label} already didn't need.
}
