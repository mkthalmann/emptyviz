% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme-bayes-bf-forest.R
\name{plot_bf_forest}
\alias{plot_bf_forest}
\title{Log Bayes Factor forest plot}
\usage{
plot_bf_forest(
  data,
  contrast,
  log_bf,
  se = NULL,
  pairs = NULL,
  contrast_strip = " NA$",
  contrast_reorder = TRUE,
  positive_color = NULL,
  negative_color = NULL,
  positive_shape = 16,
  negative_shape = 17,
  point_size = 3,
  evidence_scale = TRUE,
  weak_threshold = log(3),
  weak_label = "Weak evidence region",
  direction_labels = NULL,
  secondary_axis = FALSE,
  secondary_breaks = c(1, 2, 5, 15, 50, 150),
  xlab = NULL,
  ylab = "Contrast"
)
}
\arguments{
\item{data}{A data frame with one row per contrast.}

\item{contrast}{Unquoted column with the contrast label. If \code{pairs} is
given, this is treated as bayestestR's raw \code{"<left> - <right>"} string
and run through \code{\link[=prepare_bf_contrasts]{prepare_bf_contrasts()}} first; otherwise used directly
as the display label, unchanged.}

\item{log_bf}{Unquoted column of log Bayes Factors.}

\item{se}{Optional unquoted column of standard errors (\code{NULL} omits the
errorbar - a Bayes Factor from a single \code{bayesfactor_rope()} call,
unlike a repeated bridge-sampling estimate, has no natural per-contrast
SE at all, and that's a fully supported, first-class case here, not a
fallback).}

\item{pairs}{Optional list of \code{c(left, right)} pairs; see
\code{\link[=prepare_bf_contrasts]{prepare_bf_contrasts()}}. A pair matched in reversed order gets its
sign flipped automatically (via \code{log_bf} passed as \code{prepare_bf_contrasts()}'s
\code{value}).}

\item{contrast_strip}{Passed to \code{prepare_bf_contrasts()}'s \code{strip} when
\code{pairs} is given.}

\item{contrast_reorder}{\code{TRUE} (default) sorts rows by \code{log_bf}; \code{FALSE}
keeps \code{pairs}' own order (or \code{data}'s own order, if \code{pairs} wasn't
used).}

\item{positive_color, negative_color}{Colors for positive/negative
contrasts; default to \code{mt_colors[1]}/\code{mt_colors[2]}.}

\item{positive_shape, negative_shape}{Point shapes for positive/negative
contrasts. Color and shape both carry sign, redundantly, so direction
stays legible under grayscale printing or for red/green-blind readers.}

\item{point_size}{Size of the contrast points.}

\item{evidence_scale}{\code{TRUE} (default) bundles \code{\link[=layer_bf_evidence_scale]{layer_bf_evidence_scale()}}
in automatically, with \code{range} computed from \code{data} itself; \code{FALSE} for
a bare forest plot with no annotation.}

\item{weak_threshold, weak_label}{Passed to \code{layer_bf_evidence_scale()}
when \code{evidence_scale = TRUE}.}

\item{direction_labels, secondary_axis, secondary_breaks}{Passed through to
\code{layer_bf_evidence_scale()} when \code{evidence_scale = TRUE}.}

\item{xlab, ylab}{Axis labels.}
}
\value{
A \code{ggplot} object.
}
\description{
One row per contrast, point (+/- optional SE), colored by sign, with
\code{\link[=layer_bf_evidence_scale]{layer_bf_evidence_scale()}}'s weak-evidence-region annotation bundled in
by default.
}
\examples{
bf_data <- data.frame(
  contrast = c("A vs. B", "C vs. D", "E vs. F"),
  log_bf = c(8.2, 1.1, -0.4)
)
plot_bf_forest(
  bf_data,
  contrast = contrast,
  log_bf = log_bf,
  direction_labels = c("supports difference", "supports equivalence")
)
}
